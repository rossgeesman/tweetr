<% if @tweets %>
  <% @tweets.each do |tweet|%>
    <%= render partial: 'tweet', locals: { text: tweet.text, time_stamp: tweet.created_at} %>
  <% end %>
<% else %>

  <p>No recent tweets for <%= @handle %></p>

<% end %>